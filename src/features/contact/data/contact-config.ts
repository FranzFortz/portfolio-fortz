import type { ContactConfig } from "../types";

export const CONTACT_CONFIG: ContactConfig = {
  publicEmail: undefined,
  formActionPath: "/api/contact",
  availabilityNote: "Configure in src/features/contact/data/contact-config.ts.",
};
