# Public site URL (absolute). Used for metadata, Open Graph, sitemap, and robots.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: wire contact Server Action to email (e.g. Resend) — do not commit real keys.
# RESEND_API_KEY=
# CONTACT_TO_EMAIL=
